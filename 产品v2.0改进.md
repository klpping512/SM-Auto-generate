# SA-LogiFlow — Claude Code 项目指南

## 项目概述

SA-LogiFlow 是面向南非物流企业的 **AI 社媒营销运营平台**。核心价值：帮助物流公司用更低人力成本批量产出高质量社媒内容。

**技术栈**: Python 3.10+ / FastAPI / SQLite / Jinja2 / 慧媒CLI
**运行环境**: macOS ARM64 本地部署，端口 8080
**项目路径**: ~/Desktop/distribution-manager/
**AI 模型**: DeepSeek API（sk-f407 开头的 key）

---

## 当前架构

```
distribution-manager/
├── app.py              ← FastAPI 主入口 + 所有路由
├── database.py         ← SQLite 连接与初始化
├── models.py           ← SQLAlchemy ORM 模型
├── ai_engine.py        ← DeepSeek AI 内容生成引擎
├── publisher.py        ← 慧媒 CLI 发布调度
├── topic_library.py    ← 6大物流主题库
├── requirements.txt    ← Python 依赖
├── start.sh            ← 启动脚本
├── data/               ← SQLite 数据库文件
├── static/             ← 前端 HTML 页面（纯 HTML + JS，无框架）
│   ├── home.html       ← Dashboard 首页
│   ├── editor.html     ← AI 内容编辑器
│   ├── queue.html      ← 发布队列
│   ├── accounts.html   ← 账号管理
│   └── config.html     ← 平台配置
└── docs/               ← 文档
```

**关键设计决策**:
- 前端是纯 HTML + 原生 JS，不用 React/Vue，通过 Jinja2 模板渲染
- 数据库用 SQLite，文件存储在 data/ 目录
- 发布通过慧媒 CLI（huimei 命令行工具）调用
- DeepSeek API 直接 HTTP 调用，不用 SDK

---

## 现状评估（已完成 vs 待改进）

| 维度 | 状态 | 说明 |
|------|------|------|
| AI 内容生成 | ✅ 完成 | DeepSeek 驱动，6大物流主题，3种语气，多平台定制 |
| 发布队列 | ✅ 完成 | CRUD 完备，平台/状态筛选，操作直观 |
| 平台覆盖 | ⚠️ 部分 | 慧媒已接小红书/抖音，Facebook/Twitter/Reddit 待接入 |
| 多角色权限 | ❌ 缺失 | 单 Admin 模式，无法团队协作 |
| 审批流程 | ❌ 缺失 | 内容直接进队列，无审核环节 |
| 通知机制 | ❌ 缺失 | 发布结果需手动刷新，无主动推送 |
| 数据分析 | ⚠️ 基础 | Dashboard 仅展示发布量，无互动数据回流 |
| 素材管理 | ❌ 缺失 | 纯文字输出，缺少图片素材库 |

---

## 改进路线（按优先级）

### P0 — 权限与合规（企业可用前提）

#### 1. 多角色权限体系

**角色定义**:
- **运营专员**: 创建内容、提交审核，不可直接发布，不可访问平台配置和账号凭证
- **内容审核员**: 查看/修改待审内容，通过或驳回
- **管理员**: 全部权限（账号绑定、平台配置、用户管理、数据查看）

**实现要点**:
- FastAPI 增加 JWT + RBAC 中间件
- 前端菜单和操作按钮按角色显隐
- 数据库增加 users 表（id, username, password_hash, role, created_at）
- 角色枚举: `admin`, `editor`, `reviewer`

#### 2. 内容审批流

**状态机**: 草稿 → 待审核 → 已通过 → 已排队 → 已发布

**实现要点**:
- content 表增加字段: `status`, `reviewer_id`, `review_note`, `reviewed_at`
- 驳回时必须填写原因，内容回到草稿状态
- 新增「审核中心」页面（static/review.html）
- 状态变更记录操作人和时间

#### 3. 操作日志与凭证加密

**实现要点**:
- 新增 audit_logs 表（id, user_id, action, target, detail, ip, created_at）
- Facebook Token、Cookie 等凭证用 AES-256 加密存储
- 登录超时 8 小时，管理员可强制登出

---

### P1 — 稳定性与可观测性

#### 4. 发布失败告警与重试

**实现要点**:
- 慧媒 CLI 调用失败后自动重试：间隔 5 分钟，最多 3 次
- 超出重试后标记「失败」并推送告警
- 告警渠道：企业微信 Webhook（优先）或邮件（SMTP smtp.163.com:465）
- 错误分类：网络超时 / 账号封禁 / 内容违规

#### 5. 发布结果主动通知

**实现要点**:
- 成功通知：每条发布成功后推送 `[平台] [标题] 发布成功 ✅` + 帖子链接
- 日报摘要：每日 17:00 推送当日统计（发布N篇、平台分布、失败M篇）
- 周报摘要：每周一 09:00 AI 生成上周运营概况
- 通知渠道配置放在「平台配置」页面

#### 6. 定时发布与日历视图

**实现要点**:
- 队列卡片增加「发布时间」字段（日期时间选择器）
- 注意南非时区 UTC+2
- 最佳发布时间提示：小红书 19:00-22:00，Facebook 工作日 10:00-12:00
- 新增「发布日历」页面（static/calendar.html），用 FullCalendar.js 实现
- 后端用 APScheduler cron 任务调度

---

### P2 — 内容生产提效

#### 7. 图片素材库

- 预置南非物流场景图（德班港、货柜车、清关现场等），按主题分类
- 支持团队上传自有图片，统一存储
- 水印模板：公司 Logo，三种位置（左下/右下/对角线）
- 平台适配裁切：小红书 3:4，抖音 9:16，Facebook 4:5

#### 8. 内容模板库与批量生成

- 支持将效果好的内容保存为模板
- 「本周计划」功能：选择主题组合，一键生成全部草稿
- 队列卡片增加「一键复制文案」按钮
- AI 改写：已生成内容支持一键改写，避免多账号雷同

#### 9. 多语言与本地化

- 小红书/抖音生成中文，Facebook/Twitter/LinkedIn 生成英文
- 一键翻译功能（中文→英文）
- AI 提示词加入南非本地上下文（兰特汇率、海关法规）

---

### P3 — 数据驱动增长

#### 10. 互动数据回流

- 回流指标：阅读量、点赞数、评论数、转发数、私信询盘
- 采集：Facebook/Twitter/Reddit 通过 API 定时拉取；小红书/抖音通过慧媒 CLI
- 每 24 小时拉取一次，存入 SQLite
- Dashboard 增加「内容效果排行榜」

#### 11. 内容效果归因与策略优化

- 话题归因：统计各物流主题的平均互动率，标注「高效话题」
- 询盘归因：小红书内容植入 UTM 参数，联动 CRM
- 最佳时间分析：基于历史数据推荐各平台最佳发布时间
- 每月生成「内容策略报告」

---

## 快速改进（1-2天内可完成）

以下 3 项工作量小但体验提升明显，建议在 P0 之前优先完成：

1. **队列卡片「一键复制」**（0.5天）— 复制图标，点击复制文案+标签到剪贴板
2. **发布时间选择器**（1天）— 队列卡片增加日期时间输入框
3. **Dashboard 周报卡片**（1天）— 顶部增加「本周概况」：发布N篇、最活跃平台、AI成功率

---

## UI/UX 设计规范

**设计语言**: Linear + Stripe + Notion 风格

**色彩系统**:
```
Primary:    #2563EB  （主色调蓝）
Background: #F8FAFC  （浅灰白底）
Success:    #22C55E  （成功绿）
Warning:    #F59E0B  （警告黄）
Error:      #EF4444  （错误红）
```

**字体**: Inter（英文）+ HarmonyOS Sans（中文）
**组件风格**: 16px 圆角、大留白、Lucide Icons、ECharts 图表
**整体风格**: 苹果极简，白底/浅灰底，Arial Black 标题，大量留白，无重阴影

**导航结构**（10个页面）:
1. 首页（经营驾驶舱 Dashboard）
2. AI 工作区（三栏布局：知识库 | AI对话编辑 | 多平台预览）
3. 内容资产（Notion 风格，搜索/标签/版本/AI改写）
4. 发布日历（拖拽排期，周/月视图）
5. 数据分析（平台数据、AI复盘、ROI）
6. 热点中心（物流热点采集 + AI 选题生成）
7. 知识库（官网/PDF/FAQ/案例上传，AI 基于企业知识生成）
8. 素材中心（图片/视频/Logo/模板管理）
9. 团队协作（权限、审批、评论）
10. 设置（账号管理、平台配置、通知配置）

---

## 实施路线图

| 阶段 | 周期 | 核心目标 | 主要功能 |
|------|------|----------|----------|
| 第一阶段 | 1-2 个月 | 企业可用基础 | 多角色权限 + 审批流 + 操作日志 + 凭证加密 |
| 第二阶段 | 3-4 个月 | 稳定与可观测 | 失败告警 + 通知推送 + 定时发布 + 日历视图 |
| 第三阶段 | 5-6 个月 | 内容生产提效 | 图片素材库 + 模板共享 + 批量生成 + 多语言 |
| 第四阶段 | 6 个月+ | 数据驱动增长 | 互动数据回流 + 效果归因 + 策略优化 |

**关键里程碑**:
- 第一阶段结束 → 可交付运营团队日常使用，权责清晰，合规可追溯
- 第二阶段结束 → 系统可无人值守运行，自动通知，管理层信任度提升
- 第三阶段结束 → 效率达标（每周 30-50 篇内容，2-3 小时人力）
- 第四阶段结束 → 从执行工具进化为增长工具，驱动询盘增长

---

## 编码规范

**Python**:
- FastAPI 路由按模块拆分（目前全在 app.py，改进时应拆分）
- 数据库操作用 SQLAlchemy ORM，不用裸 SQL
- 错误处理：捕获异常返回 JSON 错误响应，不暴露堆栈
- 日志用 Python logging 模块

**前端**:
- 纯 HTML + 原生 JS，不用前端框架
- 样式用 Tailwind CSS（CDN 引入）
- 图表用 ECharts
- 日历用 FullCalendar.js
- 图标用 Lucide Icons

**数据库**:
- SQLite 文件存储在 data/ 目录
- 表结构变更加字段时，写 migration 脚本或在 database.py 的 init 中处理
- 敏感字段（Token、Cookie）必须 AES-256 加密

**Git**:
- 主分支 main
- 功能分支命名: feat/xxx, fix/xxx
- 提交信息: 中文描述，简洁明了

---

## 常用命令

```bash
# 启动服务
cd ~/Desktop/distribution-manager
python3 app.py
# 或
bash start.sh

# 安装依赖
pip3 install -r requirements.txt

# 访问
# http://localhost:8080
```

---

## 注意事项

- **不要用 Homebrew**，用户机器没装
- **pip 超时**用清华镜像: `--index-url https://pypi.tuna.tsinghua.edu.cn/simple`
- **南非时区**: UTC+2，定时发布需注意时区转换
- **慧媒 CLI**: 小红书/抖音发布依赖慧媒命令行工具，需确认已安装且 cookie 有效
- **DeepSeek API**: key 在环境变量或 config 中，不要硬编码到代码里
- **敏感词**: Claude → 克莱德，ChatGPT → GPT（小红书平台规避）
- **SQLite 并发**: FastAPI 是异步的，但 SQLite 不支持高并发写入，注意用连接池或 WAL 模式
