## 2026-08-12 13:36:59 CST｜批22源码补齐后15到50条 Hook执行文档

### 本次目标
为云端执行 Agent 提供一份可直接执行的批22文档：先补齐云端/本地缺失的热点生产源码，再将合格 Hook 从当前约 15 条推进到至少 50 条，并建立每 3 天至少新增 10 条、每 12 天清理上一个完整 12 天周期的长期运行规则。

### 改动前的问题或现状
- 本地周期清理模块、调度器接入和专项测试已存在，专项测试 7 passed，语法检查通过。
- 本地仍缺 `media_retention.py` 与 `scripts/audit_timely_hook_target.py` 等完整生产链路文件；云端 SSH/域名可达性此前不稳定，因此不能在不完整源码上宣称补库完成。
- 现有 15→50 的实际采集、下载、ffprobe、模型分析、三帧视觉复核和数据库审计尚未闭环。

### 已完成改动
- 新增 `docs/总指挥指令-2026-08-12/批22-源码补齐后15到50条Hook-执行指令.md`。
- 文档分为源码恢复、周期清理预检、15→50 四批补库、长期三天节奏、12天周期清理、验收回报和禁止事项七部分。
- 明确禁止创建空壳脚本、禁止把媒体候选直接计为合格 Hook、禁止用素材年龄配置冒充完整周期清理，并要求保留母片、来源快照、三帧证据、Buffalo 原有素材和活动项目引用。
- 明确清理开关在源码补齐和 dry-run 验收前保持关闭；只有当前周期达到至少 40 条、四批记录、证据/备份/审计齐全后，才允许执行上一个完整周期的删除。

### 涉及的主要文件
- `docs/总指挥指令-2026-08-12/批22-源码补齐后15到50条Hook-执行指令.md`
- `docs/改进日志/2026-08.md`
- Obsidian 同步副本：`/Users/ylanlll/Desktop/klpping/知识库/项目/Distribution Manager/总指挥指令-2026-08-12/批22-源码补齐后15到50条Hook-执行指令.md`

### 构建、测试和浏览器验证结果
- 文档静态检查：通过，451 行，`git diff --check` 通过。
- 已知专项结果：`pytest -q tests/test_hotspot_hook_cycle_cleanup.py` 为 7 passed；`python3 -m py_compile hotspot_hook_cycle_cleanup.py scheduler.py` 通过。
- 本次只新增执行文档和日志，未执行云端下载、清理、数据库删除、服务重启或浏览器验证。

### Git 提交和推送状态
- 当前待提交；计划只提交本执行文档和本项目日志，不提交 `.env`、`.env.save`、数据库、生成素材或其他未授权工作树改动。
- 不推送远程 Git 仓库；云端代码不因本条文档自动改变。

### 尚未完成或需要人工操作的事项
- 先恢复完整云端源码并确认 SSH 可达，再按文档执行 15→50；在此之前不得把 7 条清理测试通过视为生产链路通过。
- `.env.save` 属于敏感配置备份，不能提交或上传；其中涉及的密钥应由用户在安全位置轮换后再删除该备份。
