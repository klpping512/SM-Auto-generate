# 批19 返工执行指令 v3（第二轮返工）

日期：2026-08-10
状态：第二轮验收 **FAIL**。qcoder 报告的"全部完成"与工作区实际不符。本指令为**最终版**，给出逐文件、逐函数的**终稿代码**。
纪律要求（必须遵守，否则直接返工）：**以下代码是终稿，逐字符转写，不得改名、换序、改写、加戏、自行"优化"或换一种实现。** 若某处你看不懂或想改，先照抄，然后在交付说明里写问题，我来裁决。

---

## 一、第二轮复核结论（我独立查的工作区，附证据）

| 模块 | 结论 | 证据 |
|---|---|---|
| P4 media_retention | ✅ | 已回退（git diff 为空） |
| P4 admin prefix | ✅ | `prefix="/api/admin"` |
| P4 scheduler/database | ✅ | misfire + 启动补跑 + created_at 均保留 |
| P4 admin **asyncio** | ❌ | `routes/admin.py` 用了 `await asyncio.to_thread(...)` 但**没有 `import asyncio`** → 调用 hook 清理端点时 `NameError` |
| P4 admin 路径 | ⚠️ | 仍是 `/cleanup/run`、`/hook-cleanup/run`，未按指令对齐为 `/media-cleanup`、`/hotspot-hook-library/cleanup` |
| P2 字幕对齐 | ❌ **整个功能没接上** | `_build_subtitle_cues_internal` 被删除；渲染调用点（~1524）改回无音频的 `build_subtitle_cues`；`_detect_silence_points`/`_align_cues_to_silence` 成了**死代码**（无人调用）；分句正则丢了中国标点 `[，。！？；]`（中文旁白不再分句，回归）；`_align_cues_to_silence` 内部算法也不符合设计（多短句+停顿场景退化为字符比例，边界落在静音起点） |
| P1 冷却 | ❌ | tie-break（P1-C）**完全没实现**：sort 仍是 `int(item["hotspot_id"])`，没有 `_tiebreak` 字段，`random` 导了没用；`_usage_freshness_penalty(published_ts)` 传错参数（应传 `last_used_at`）；还私自加了 `*2` 倍率 |
| P3 入队 | ❌ | 端点路径 `/api/video-projects/enqueue`（无路径参数）与前端 `/api/video-projects/{id}/enqueue` 不匹配→404；**再次用了不存在的 `db.execute` 和不存在的 `publish_queue` 表**；`from models import VideoGenerationJobStatus`（该类不存在）→ImportError；`datetime.now(timezone.utc)`（`timezone` 未导入）→NameError；`scheduled_at` 用 ISO 带 `T` 格式 → `get_scheduled_items`（`"%Y-%m-%d %H:%M"` 字符串比较）永远认为未到时间 → **立即发布永远不会自动发**；未做项目属主/账号校验 |

---

## 二、修复顺序与文件清单

按 P4 收尾 → P2 → P1 → P3 执行。涉及文件：
- `routes/admin.py`（P4 两处）
- `video_renderer.py`（P2 整块）
- `app.py`（P1 两块）
- `models.py`（P3 模型）
- `routes/video_generation_routes.py`（P3 端点 + import）
- `static/video-project.html`（P3 前端 body）

---

## 三、P4 收尾（2 处）

### P4-a：`routes/admin.py` 顶部加 `import asyncio`

在 `import os` 那一行下面加：
```python
import asyncio
```

### P4-b：`routes/admin.py` 两个路径对齐

把：
```python
@router.post("/cleanup/run")
```
改为：
```python
@router.post("/media-cleanup")
```
把：
```python
@router.post("/hook-cleanup/run")
```
改为：
```python
@router.post("/hotspot-hook-library/cleanup")
```
其余（prefix、函数体、require_role、os.environ、to_thread）保持不变。

---

## 四、P2：字幕静音对齐（video_renderer.py 终稿）

**删除**当前 `build_subtitle_cues`（:889）整段、`_detect_silence_points`（:925）整段、`_align_cues_to_silence`（:970）整段，**用下面四个函数原样替换**。分句逻辑统一收敛到 `_build_subtitle_cues_internal`。

### 4.1 `build_subtitle_cues`（公共入口，保持签名不变）

```python
def build_subtitle_cues(text: str, duration: float) -> list[dict]:
    """按语义停顿分句并按字符比例分配时长；无音频时的默认实现。"""
    return _build_subtitle_cues_internal(text, duration, audio_path=None)
```

### 4.2 `_build_subtitle_cues_internal`（分句 + 音频对齐）

```python
def _build_subtitle_cues_internal(
    text: str,
    duration: float,
    *,
    audio_path: Path | None = None,
    silence_threshold: float = -50.0,
    min_pause_duration: float = 0.3,
    ffmpeg: str = "ffmpeg",
) -> list[dict]:
    """构建字幕时间轴：有音频时用 ffmpeg silencedetect 把边界吸到真实语音段；无音频时字符比例。"""
    parts = [p.strip() for p in re.split(r"(?<=[，。！？；,.!?;])", text or "") if p.strip()]
    if not parts and text.strip():
        parts = [text.strip()]
    compact_parts = []
    for part in parts:
        remaining = part
        while len(remaining) > 36:
            cut = remaining.rfind(" ", 0, 37)
            if cut < 18:
                cut = 36
            compact_parts.append(remaining[:cut].strip())
            remaining = remaining[cut:].strip()
        if remaining:
            compact_parts.append(remaining)
    parts = compact_parts
    total = sum(max(1, len(part)) for part in parts) or 1
    cursor, cues = 0.0, []

    if not audio_path or not os.path.exists(audio_path):
        for index, part in enumerate(parts):
            end = duration if index == len(parts) - 1 else cursor + duration * max(1, len(part)) / total
            cues.append({"start": round(cursor, 3), "end": round(end, 3), "text": part})
            cursor = end
        return cues

    silence_points = _detect_silence_points(str(audio_path), silence_threshold, min_pause_duration, ffmpeg=ffmpeg)
    if silence_points:
        return _align_cues_to_silence(parts, duration, silence_points, tolerance=0.12)

    for index, part in enumerate(parts):
        end = duration if index == len(parts) - 1 else cursor + duration * max(1, len(part)) / total
        cues.append({"start": round(cursor, 3), "end": round(end, 3), "text": part})
        cursor = end
    return cues
```

### 4.3 `_detect_silence_points`（带 ffmpeg 参数）

```python
def _detect_silence_points(
    audio_path: str,
    threshold: float = -50.0,
    min_duration: float = 0.3,
    ffmpeg: str = "ffmpeg",
) -> list[tuple[float, float]]:
    """用 ffmpeg silencedetect 检测静音段，返回 (start, end) 列表。"""
    try:
        cmd = [
            ffmpeg,
            "-i", audio_path,
            "-af", f"silencedetect=noise={threshold}dB:d={min_duration}",
            "-f", "null", "-",
        ]
        result = subprocess.run(
            cmd, capture_output=True, text=True, timeout=60,
            stderr=subprocess.PIPE, encoding="utf-8", errors="replace",
        )
        stderr = result.stderr or ""
        starts = [float(m.group(1)) for m in re.finditer(r"silence_start: ([\d.]+)", stderr)]
        ends = [float(m.group(1)) for m in re.finditer(r"silence_end: ([\d.]+)", stderr)]
        points = []
        si = ei = 0
        while si < len(starts) and ei < len(ends):
            if starts[si] < ends[ei]:
                points.append((starts[si], ends[ei]))
                si += 1
                ei += 1
            else:
                ei += 1
        return points
    except Exception:
        logger.warning("静音检测失败：%s", audio_path, exc_info=True)
        return []
```

### 4.4 `_align_cues_to_silence`（语音时间轴映射，终稿）

```python
def _align_cues_to_silence(
    parts: list[str],
    total_duration: float,
    silence_points: list[tuple[float, float]],
    tolerance: float = 0.12,
) -> list[dict]:
    """语音时间轴映射：把字符比例边界投影到真实语音段，保证每个 cue 不跨静音、末条 end=总时长。"""
    speech: list[tuple[float, float]] = []
    cursor = 0.0
    for start, end in sorted(silence_points, key=lambda x: x[0]):
        if start > cursor + tolerance:
            speech.append((cursor, min(start, total_duration)))
        cursor = max(cursor, end)
    if cursor < total_duration - tolerance:
        speech.append((cursor, total_duration))

    total_chars = sum(max(1, len(p)) for p in parts) or 1

    # 无有效语音段 → 回退字符比例
    if not speech:
        cur = 0.0
        cues = []
        for i, part in enumerate(parts):
            end = total_duration if i == len(parts) - 1 else cur + total_duration * max(1, len(part)) / total_chars
            cues.append({"start": round(cur, 3), "end": round(end, 3), "text": part})
            cur = end
        return cues

    speech_total = sum(e - s for s, e in speech)
    cum = 0.0
    boundaries_sec = [0.0]
    for p in parts[:-1]:
        cum += max(1, len(p))
        boundaries_sec.append(speech_total * cum / total_chars)

    def sec_to_real(sec: float) -> float:
        for s, e in speech:
            span = e - s
            if sec <= span:
                return s + sec
            sec -= span
        return speech[-1][1]

    cues = []
    for i, part in enumerate(parts):
        start = sec_to_real(boundaries_sec[i])
        end = sec_to_real(boundaries_sec[i + 1]) if i + 1 < len(parts) else speech[-1][1]
        cues.append({
            "start": round(max(start, 0.0), 3),
            "end": round(min(max(end, start + 0.05), total_duration), 3),
            "text": part,
        })
    return cues
```

### 4.5 渲染调用点（video_renderer.py ~:1523）

把：
```python
            audio_path = wav if os.path.exists(wav) else None
            cues = build_subtitle_cues(scene["voiceover"], min(speech_duration, duration))
```
改为：
```python
            audio_path = wav if os.path.exists(wav) else None
            cues = _build_subtitle_cues_internal(
                scene["voiceover"],
                min(speech_duration, duration),
                audio_path=audio_path,
                ffmpeg=ffmpeg or "ffmpeg",
            )
```
（`ffmpeg` 在 ~:1306 已由 `shutil.which` 解析，作用域内可用。）

---

## 五、P1：素材复用冷却（app.py 终稿）

### 5.1 确认 `import random` 在 app.py 顶部（应已有）

### 5.2 替换 app.py `_marketing_hook_candidates` 中这段（原 :2095-2138，**整段替换**）

```python
        # P1: 素材使用冷却（从 assets 表读 usage_count/last_used_at，复用 planner 惩罚）
        hook_asset_id = hooks[0].get("asset_id") if hooks else None
        usage_count = 0
        last_used_at = None
        if hook_asset_id:
            _asset_row = db.get_asset(int(hook_asset_id))
            if _asset_row:
                usage_count = int(_asset_row.get("usage_count") or 0)
                last_used_at = _asset_row.get("last_used_at")
        usage_penalty = min(usage_count, 5) + (
            hotspot_video_planner._usage_freshness_penalty(last_used_at) if last_used_at else 0.0
        )

        candidates.append({
            "hotspot_id": hotspot["id"], "event_clip_id": hooks[0]["event_clip_id"] if hooks else None,
            "title": headline[:200],
            "summary": (str(hotspot.get("summary_zh") or hotspot.get("summary") or "") + " " + hook_context_text)[:500],
            "source_url": hotspot.get("source_url"), "published_at": hotspot.get("published_at"),
            "published_ts": published_ts,
            "hook_type": "direct" if direct else "contextual", "logistics_signal": kind,
            "logistics_scenes": sorted(event_profile),
            "relevance": {
                "level": "strong_direct" if direct else "strong_logistics_context",
                "reason": (
                    "热点事实与用户问题存在直接物流节点重合。"
                    if direct else
                    "热点现场可直接解释当前物流节点的上游风险或准备动作。"
                ),
            },
            "marketing_question": question,
            "hook_clips": hooks, "can_render_video": len(hooks) >= 1,
            "score": (
                direct * 40
                + profile_overlap * 16
                + event_fit * 5
                + intent_bridge
                + reuse_bias
                - mismatch_penalty
                + freshness_bonus
                - usage_penalty
            ),
            "_tiebreak": random.random(),
            "usage_boundary": "热点只用于提出问题或解释外部背景；Buffalo 服务能力只能由知识库、品牌证据和自有镜头说明。",
            "reuse_policy": "只有该 Hook 是当前主题最强事实现场时才复用；若存在同等强相关候选，优先选择不同事件，避免连续视频看起来都用同一个开场。",
        })
    # P1: 同分随机 tie-break（分数完全相等时由随机值打破）
    candidates.sort(
        key=lambda item: (item["score"], item.get("published_ts") or 0, item.get("_tiebreak", 0.0)),
        reverse=True,
    )
```

要点（对照你上一版的三处错误）：① 传 `last_used_at`，**不是** `published_ts`；② 没有 `*2` 倍率；③ sort key 第三元是 `_tiebreak`，**不是** `int(item["hotspot_id"])`。

---

## 六、P3：发布队列入队（终稿）

### 6.1 `models.py`：把现有 `VideoProjectEnqueueRequest`（:398）整体替换

```python
class VideoProjectEnqueueRequest(BaseModel):
    platforms: list[str] = Field(default=[], description="要发布的平台（Platform 枚举值字符串，如 douyin/xiaohongshu）")
    account_targets: dict[str, list[int]] = Field(default={}, description="平台 -> 目标账号主键列表")
    scheduled_at: Optional[str] = Field(None, description="计划发布时间（ISO 文本）；留空=立即发布")
    title: Optional[str] = None
```

### 6.2 `routes/video_generation_routes.py`：models import 块加 `Platform`

把：
```python
from models import (
    UserRole,
```
改为：
```python
from models import (
    Platform,
    UserRole,
```

### 6.3 替换整个 enqueue 端点（原 :433 起，含路由装饰器）

```python
    @router.post("/api/video-projects/{project_id}/enqueue")
    async def enqueue_video_project(
        project_id: str,
        request: VideoProjectEnqueueRequest,
        user=Depends(get_current_user),
    ) -> dict:
        """将已完成渲染的视频项目加入发布队列（立即或定时）。"""
        project = db.get_video_project(project_id, created_by=user["id"])
        if not project:
            raise HTTPException(status_code=404, detail="Project not found")
        job = db.get_video_generation_job(project.get("active_job_id"))
        if not job or job.get("status") != "succeeded" or not job.get("output_path"):
            raise HTTPException(status_code=400, detail="成片尚未就绪，无法入队")

        now_str = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        scheduled_at = request.scheduled_at or now_str
        platforms = request.platforms or [project.get("platform", "douyin")]
        valid_platforms = {p.value for p in Platform}
        for platform in platforms:
            if platform not in valid_platforms:
                raise HTTPException(status_code=400, detail=f"不支持的平台：{platform}")

        created_ids = []
        for platform in platforms:
            target_ids = request.account_targets.get(platform) or [None]
            for target_id in target_ids:
                if target_id is not None:
                    account = db.get_account(target_id)
                    if not account or account.get("owner_id") != user["id"]:
                        raise HTTPException(status_code=403, detail="不能操作其他用户的账号")
                    if account.get("platform") != platform:
                        raise HTTPException(status_code=400, detail="目标账号与发布平台不匹配")
                queue_id = db.add_to_queue(
                    title=request.title or project.get("title") or "",
                    body="",
                    platform=platform,
                    scheduled_at=scheduled_at,
                    status="queued",
                    created_by=user["id"],
                    attachments=[{"type": "video", "path": job["output_path"]}],
                    target_account_id=target_id,
                )
                created_ids.append(queue_id)

        db.add_audit_log(
            user["id"], user["username"], "enqueue_video_project",
            target=project_id, detail=json.dumps({"queue_ids": created_ids}),
        )
        return {"status": "queued", "queue_ids": created_ids, "message": f"已入队 {len(created_ids)} 条"}
```

对照你上一版的错误：① 路径带 `{project_id}` 路径参数（匹配前端）；② **没有 `db.execute`、没有 `publish_queue`**，用 `db.add_to_queue` 写 `queue` 表；③ 不 import `VideoGenerationJobStatus`，状态直接字符串比较 `"succeeded"`；④ `scheduled_at` 用 `"%Y-%m-%d %H:%M:%S"`（与 `get_scheduled_items` 的字符串比较兼容），不是 ISO `T` 格式；⑤ 项目属主经 `db.get_video_project(project_id, created_by=user["id"])` 校验；⑥ 经 `project["active_job_id"]` 取 job。

### 6.4 前端 `static/video-project.html`：`enqueueToPublishQueue` 加 body

把当前函数体（只 `{method:'POST'}`）替换为：

```js
async function enqueueToPublishQueue(){
  if(!project?.id){showToast('项目 ID 无效','error');return;}
  try{
    const payload={
      platforms:[project?.platform || 'douyin'],
      account_targets:{},
      scheduled_at:null,
    };
    const response=await apiFetch(`/api/video-projects/${project.id}/enqueue`,{
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body:JSON.stringify(payload),
    });
    const result=await response.json();
    if(response.ok){
      showToast('✅ 已成功加入发布队列，系统将在 1 分钟内自动发布');
      setTimeout(()=>location.reload(),1500);
    }else{
      showToast('❌ '+(result.detail||'入队失败'),'error');
    }
  }catch(error){
    showToast('❌ 加入失败：'+error.message,'error');
  }
}
```

按钮触发条件（status==='succeeded' && job?.output_url && publish_allowed===true）保持不动。

---

## 七、强制自检（必须把命令和输出原样回贴，不许只写"通过"）

VM 跑不了 pytest/fastapi，用直接 import + 静态 grep。以下命令逐条执行，把输出贴回。

```bash
# P2-1：对齐函数实测（边界必须 ≠ 字符比例，且末条 end == duration）
cd /Users/ylanlll/Desktop/商务部/distribution-manager
python3 -c "
import ast
src=open('video_renderer.py',encoding='utf-8').read()
tree=ast.parse(src)
def getfunc(name):
    for n in tree.body:
        if isinstance(n,ast.FunctionDef) and n.name==name:
            return ast.get_source_segment(src,n)
ns={'build_subtitle_cues':lambda t,d:[]}
exec(compile(getfunc('_align_cues_to_silence'),'<s>','exec'),ns)
f=ns['_align_cues_to_silence']
print(f(['句一','句二','句三'],9.0,[(3.0,3.3),(6.0,6.3)]))
"
# 期望输出形如：start 边界 2.8/5.9 附近（落在语音段内），末条 end==9.0；绝不该是 [(0,3),(3,6),(6,9)]

# P2-2：死代码检查——渲染路径必须用 _build_subtitle_cues_internal
grep -n "_build_subtitle_cues_internal(" video_renderer.py   # 期望：内部定义 + 渲染调用点，共≥2处
grep -n "def _detect_silence_points\|def _align_cues_to_silence" video_renderer.py   # 期望：两处定义仍在且被 _build_subtitle_cues_internal 调用
grep -n "\[\，。！？；,.!?;\]" video_renderer.py   # 期望：中文标点正则已恢复

# P1：tie-break 与冷却参数
grep -n "_tiebreak\|_usage_freshness_penalty(last_used_at)" app.py   # 期望：_tiebreak 出现（字段+排序key共≥2处）；惩罚传 last_used_at
grep -n "published_ts)" app.py | grep -n "usage_freshness" || echo "OK: 无 _usage_freshness_penalty(published_ts)"

# P3：端点与表
grep -n "@router.post(\"/api/video-projects/{project_id}/enqueue\")" routes/video_generation_routes.py   # 期望：带路径参数
grep -n "db.execute\|publish_queue" routes/video_generation_routes.py   # 期望：无输出
grep -n "db.add_to_queue\|get_video_generation_job\|get_video_project" routes/video_generation_routes.py   # 期望：各≥1
grep -n "VideoGenerationJobStatus" routes/video_generation_routes.py   # 期望：无输出
grep -n "import asyncio" routes/admin.py   # 期望：有
grep -n "@router.post(\"/media-cleanup\")\|@router.post(\"/hotspot-hook-library/cleanup\")" routes/admin.py   # 期望：两行都在
```

---

## 八、回归与提交

回归文件（宿主如有 pytest 则跑，否则说明）：`test_video_generation_ui.py`、`test_chat_intent.py`、`test_hotspot_hook_library_gates.py`、`test_media_retention.py`、`test_matching_diagnostics.py`、`test_hotspot_event_clips.py`。

提交注明"批19 返工 v3"。**先回 diff + 自检输出，我复核通过才算完成。**
