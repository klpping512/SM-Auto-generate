# 批19 返工执行指令 — 补充（续 P4 后）

日期：2026-08-10
前置：qcoder 已中断并汇报"Step 0 + P4 完成，P2/P1/P3 缺实现细节"。本补充指令给出：
1. P4 已做部分的独立复核结论（部分通过 + 4 处待修）；
2. P2/P1/P3 被卡住的问题逐条解答 + **完整可粘贴代码**。
执行顺序：**先修 P4 待修项 → P2 → P1 → P3**。

---

## 一、P4 复核结论（我独立查了工作区，非只看你的报告）

### ✅ 已通过
| 项 | 证据 |
|---|---|
| P0 Step 0 | `git diff -- hotspot_event_matching.py` 为空，`os.environ`/`db.execute` 无残留 |
| P4-1 misfire | scheduler.py:685 `check_scheduled_publish` 已加 `misfire_grace_time=120`；两个清理 cron 各加 `misfire_grace_time=3600` |
| P4-2 启动补跑 | `asyncio.create_task`（30s 后调 `scheduler.cleanup_hotspot_hook_library`）。**可用**——`start_scheduler()` 在 app.py:178 lifespan 内调用，有运行中事件循环；且该 wrapper 非 dry-run（scheduler.py:450 直接 `to_thread` 调真实清理） |
| P4-3 年龄基准 | database.py:3390-3392 两处 WHERE + ORDER BY 均已 `datetime(created_at)`，全文件无 `COALESCE(confirmed_at,created_at)` 残留 |
| P4-4 部分 | admin.py 三个函数有 `@router.*` 装饰器；依赖 `require_role(UserRole.ADMIN)`；配置读 `os.environ`；无 `db.get_admin_token`/`db.get_config` |

### ⚠️ 待修（P4-5 ~ P4-8）

**P4-5（必须回退）media_retention.py `_asset_is_due`**：设计明确"media_retention.py 无需改"（§4.3）。但你把它改成 `base = created_at`，影响的是**自有素材媒体清理**路径（`HOTSPOT_SOURCE_RETENTION_DAYS=7`，`_cleanup` 用），不是热点 Hook 路径。原逻辑 `last_used_at or created_at` 是有意保护"近期用过"的自有素材；改成 created_at 会导致**最近使用过的自有素材被提前删除**。
```bash
git checkout -- media_retention.py
```

**P4-6（必须改）admin.py 路由前缀缺 `/api`**：全项目路由模式是"router 不带 prefix、装饰器写完整 `/api/...` 路径"（见 auth_routes/video_generation_routes 均为 `APIRouter()`）。你 admin.py 用了 `prefix="/admin"`，最终路径是 `/admin/cleanup/run` —— **不在 `/api` 下，与全站 API 不一致**。
改为：
```python
router = APIRouter(prefix="/api/admin", tags=["admin"])
```
并将三个路径与设计对齐：
```python
@router.post("/media-cleanup")                    # /api/admin/media-cleanup
@router.post("/hotspot-hook-library/cleanup")     # /api/admin/hotspot-hook-library/cleanup
@router.get("/status")                            # /api/admin/status
```

**P4-7（建议改）hook 清理在 async 里阻塞**：`manual_trigger_hotspot_hook_cleanup` 直接同步调 `media_retention.cleanup_hotspot_hook_library(...)`（做文件 IO），会卡事件循环。包 `await asyncio.to_thread(...)`；并加请求体 `dry_run`（默认 False）：
```python
from pydantic import BaseModel
class CleanupRequest(BaseModel):
    dry_run: bool = False

@router.post("/hotspot-hook-library/cleanup")
async def manual_trigger_hotspot_hook_cleanup(req: CleanupRequest, admin: dict = Depends(require_role(UserRole.ADMIN))):
    ...
    report = await asyncio.to_thread(
        media_retention.cleanup_hotspot_hook_library,
        Path(__file__).with_name("static"),
        retention_days=retention_days,
        protect_days=protect_days,
        dry_run=req.dry_run,
    )
```

**P4-8（检查）启动补跑用 `asyncio.create_task`**：功能可用，但比一次性 `date` job 弱在：若 lifespan 启动阶段异常提前退出，后台 task 可能不执行。可保留现状，但请确认 `scheduler.py` 顶部已 `import asyncio`（你在函数内 `import asyncio`，能用，但建议移到顶部）。

---

## 二、P2 返工：完整替换代码（你缺的就是这份）

`video_renderer.py` 三个改动点。**注意：你当前 `_align_cues_to_silence` 是死代码**（`non_silent_intervals` 算完没用；`for s_start, s_end in sorted_points` 里改的 `adjusted_end`/`available_start` 在循环外被 `previous_end + tolerance` 覆盖）。整个函数替换。

### 2.1 `_detect_silence_points` 加 ffmpeg 参数（不硬编码 "ffmpeg"）

改函数签名与 `cmd`：
```python
def _detect_silence_points(
    audio_path: str,
    threshold: float = -50.0,
    min_duration: float = 0.3,
    ffmpeg: str = "ffmpeg",
) -> list[tuple[float, float]]:
    ...
    cmd = [ffmpeg, "-i", audio_path,
           "-af", f"silencedetect=noise={threshold}dB:d={min_duration}",
           "-f", "null", "-"]
```
其余（subprocess.run 解析逻辑）保持不变。

### 2.2 整段替换 `_align_cues_to_silence`（语音时间轴映射）

```python
def _align_cues_to_silence(
    parts: list[str],
    total_duration: float,
    silence_points: list[tuple[float, float]],
    tolerance: float = 0.12,
) -> list[dict]:
    """语音时间轴映射：把字符比例边界投影到真实语音段上，保证 cue 不跨静音。"""
    # 1) 静音段补集 → 语音段
    speech: list[tuple[float, float]] = []
    cursor = 0.0
    for start, end in sorted(silence_points, key=lambda x: x[0]):
        if start > cursor + tolerance:
            speech.append((cursor, min(start, total_duration)))
        cursor = max(cursor, end)
    if cursor < total_duration - tolerance:
        speech.append((cursor, total_duration))

    # 无有效语音段 → 回退字符比例
    if not speech:
        total = sum(max(1, len(p)) for p in parts) or 1
        cur = 0.0
        cues = []
        for i, part in enumerate(parts):
            end = total_duration if i == len(parts) - 1 else cur + total_duration * max(1, len(part)) / total
            cues.append({"start": round(cur, 3), "end": round(end, 3), "text": part})
            cur = end
        return cues

    speech_total = sum(e - s for s, e in speech)
    # 2) 字符占比边界（0..N）→ 语音秒数 → 真实时间戳
    total_chars = sum(max(1, len(p)) for p in parts) or 1
    cum = 0.0
    boundaries_sec = [0.0]
    for p in parts[:-1]:
        cum += max(1, len(p))
        boundaries_sec.append(speech_total * cum / total_chars)

    def sec_to_real(sec: float) -> float:
        for s, e in speech:
            span = e - s
            if sec <= span:
                return s + sec
            sec -= span
        return speech[-1][1]

    cues = []
    for i, part in enumerate(parts):
        start = sec_to_real(boundaries_sec[i])
        end = sec_to_real(boundaries_sec[i + 1]) if i + 1 < len(parts) else speech[-1][1]
        cues.append({
            "start": round(max(start, 0.0), 3),
            "end": round(min(max(end, start + 0.05), total_duration), 3),
            "text": part,
        })
    return cues
```

### 2.3 `_build_subtitle_cues_internal` 透传 ffmpeg

- 签名加 `ffmpeg: str = "ffmpeg"`；
- 第 942 行调用改为：
```python
silence_points = _detect_silence_points(str(audio_path), silence_threshold, min_pause_duration, ffmpeg=ffmpeg)
```
- 渲染调用点（~1555）改为：
```python
cues = _build_subtitle_cues_internal(
    scene["voiceover"],
    min(speech_duration, duration),
    audio_path=audio_path,
    ffmpeg=ffmpeg or "ffmpeg",
)
```
（`ffmpeg` 在 1306 行由 `shutil.which` 解析，可能为 None，兜底为 "ffmpeg"；`_detect_silence_points` 内部已有 try/except，检测失败返回 [] → `_build_subtitle_cues_internal` 走字符比例回退。）

**P2 自测**（可直接 import 后跑）：
```python
from video_renderer import _align_cues_to_silence
cues = _align_cues_to_silence(["句一","句二","句三"], 9.0, [(3.0,3.3),(6.0,6.3)])
assert len(cues) == 3, cues
assert abs(cues[-1]["end"] - 9.0) < 1e-6
for c in cues:
    for s0, s1 in [(3.0,3.3),(6.0,6.3)]:
        # 不允许整个 cue 落在静音段内
        assert not (s0 + 1e-6 < c["start"] and c["end"] < s1 - 1e-6), c
print(cues)  # 边界应明显 ≠ 字符比例版
```

---

## 三、P1 返工：两个问题的答案 + 精确改动点

### 你问的两个问题

1. **`_usage_freshness_penalty` 在哪**：`hotspot_video_planner.py:333`（依赖 `_parse_utc` 在 :319）。app.py:58 **已** `import hotspot_video_planner`，直接用 `hotspot_video_planner._usage_freshness_penalty(last_used_at)`。
2. **usage 从哪张表读**：**`assets` 表**，用 `db.get_asset(int(asset_id))`（database.py:2077），返回 dict 含 `usage_count`/`last_used_at`。**不存在 `asset_usage` 表，`db.execute` 不是公开函数**——首轮错用这两个都是致命错，不要再碰。

### 改动点（app.py `_marketing_hook_candidates`，函数在 :1905）

**(a) 顶部补 import：**
```python
import random
```

**(b) 在 `candidates.append({...})`（~:2092）之前**，取素材用量：
```python
hook_asset_id = hooks[0].get("asset_id") if hooks else None
usage_count = 0
last_used_at = None
if hook_asset_id:
    _asset_row = db.get_asset(int(hook_asset_id))
    if _asset_row:
        usage_count = int(_asset_row.get("usage_count") or 0)
        last_used_at = _asset_row.get("last_used_at")
```

**(c) candidate dict 里**：加字段 `"usage_count": usage_count,`；score 表达式（当前 :2109-2117）末尾追加：
```python
- min(usage_count, 5)
- (hotspot_video_planner._usage_freshness_penalty(last_used_at) if last_used_at else 0)
```
并在 dict 内加 `"_tiebreak": random.random(),`。

**(d) 排序 key（:2121-2125）改为：**
```python
key=lambda item: (item["score"], item.get("published_ts") or 0, item.get("_tiebreak", 0.0)),
```
`reverse=True` 保持。

**(e) 不动**：`strict_terms`/`direct`/`profile_overlap`/`intent_bridge` 布尔准入门禁；`hotspot_event_matching.py`（已回退，勿再改）。

**P1 自测**：构造一个 candidate（usage=0）与自身 usage=3 同分比较，断言冷却后降序仍正确；两个完全同分候选调两次，断言 top 集合含两者。

---

## 四、P3 返工：三个问题的答案 + 完整端点

### 你问的三个问题

1. **UUID str**：`video_projects.id` 是 TEXT UUID，路径参数天然是字符串。端点签名直接 `project_id: str`（**不是 int**，这是首轮 P3 直接报错的原因）。
2. **output_path 在哪读**：在 **`video_generation_jobs` 表**，用 `db.get_video_generation_job(project["active_job_id"])`（database.py:4098）→ `job["output_path"]`。**video_projects 表没有 output_path 列**。
3. **scheduled_at 非空**：`get_scheduled_items`（database.py:1667）要求 `status='queued' AND scheduled_at IS NOT NULL AND scheduled_at <= now`，且 now 是 `"%Y-%m-%d %H:%M"` 字符串比较。**立即发布必须把 scheduled_at 填成当前时间**（不能 None），格式与库内一致（ISO 文本即可）。

### models.py 加请求模型

```python
class VideoEnqueueRequest(BaseModel):
    platforms: list[Platform] = []
    account_targets: dict[str, list[int]] = Field(default={}, description="平台 -> 目标账号主键列表")
    scheduled_at: Optional[str] = None
    title: Optional[str] = None
```
（确认 `Platform` 已在 models 顶部 `from enum import Enum` 环境下定义；`Field`/`Optional` 已导入。）

### 重写端点（routes/video_generation_routes.py，替换 :432 起的整个函数）

```python
@router.post("/api/video-projects/{project_id}/enqueue")
async def enqueue_video_project(
    project_id: str,
    req: VideoEnqueueRequest,
    user=Depends(get_current_user),
):
    project = db.get_video_project(project_id, created_by=user["id"])
    if not project:
        raise HTTPException(404, "Project not found")
    job = db.get_video_generation_job(project.get("active_job_id"))
    if not job or job.get("status") != "succeeded" or not job.get("output_path"):
        raise HTTPException(400, "成片尚未就绪，无法入队")

    now_str = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    scheduled_at = req.scheduled_at or now_str
    platforms = req.platforms or [Platform(project.get("platform", "douyin"))]
    added = []
    for platform in platforms:
        target_ids = req.account_targets.get(str(platform)) or [None]
        for target_id in target_ids:
            if target_id is not None:
                account = db.get_account(target_id)
                if not account or account.get("owner_id") != user["id"]:
                    raise HTTPException(403, "不能操作其他用户的账号")
                if account.get("platform") != str(platform):
                    raise HTTPException(400, "目标账号与发布平台不匹配")
            queue_id = db.add_to_queue(
                title=req.title or project.get("title") or "",
                body="", platform=str(platform),
                scheduled_at=scheduled_at, status="queued",
                created_by=user["id"],
                attachments=[{"type": "video", "path": job["output_path"]}],
                target_account_id=target_id,
            )
            added.append(queue_id)
    db.add_audit_log(user["id"], user["username"], "enqueue_video_project",
                     target=project_id, detail=json.dumps({"queue_ids": added}))
    return {"status": "queued", "queue_ids": added, "message": f"已入队 {len(added)} 条"}
```

要点：账号校验内联（勿引 app 的 `_account_for_user`，避免循环导入）；`platforms` 为空时用项目默认平台；`scheduled_at` 为空时填 now。

### 前端（static/video-project.html）

`enqueueToPublishQueue()` 现在 POST 无 body —— 改为带 JSON body：
```js
await apiFetch(`/api/video-projects/${project.id}/enqueue`, {
  method: 'POST',
  body: JSON.stringify({
    platforms: [selectedPlatform],          // 默认 project.platform
    account_targets: selectedAccount ? { [selectedPlatform]: [selectedAccount] } : {},
    scheduled_at: scheduledDatetime || null, // datetime-local 转 ISO；空=立即
  }),
});
```
弹窗：平台下拉（默认项目平台）、目标账号下拉（可选）、datetime-local（可选）。成功 toast + 刷新队列角标。

**P3 自测**：mock 一个 `status='succeeded'` 且带 `output_path` 的 job → 调端点 → 断言 `db.get_scheduled_items()` 能取到该条且 `attachments[0]["path"] == job["output_path"]`。

---

## 五、收尾

1. 跑绿回归：`test_media_retention.py`、`test_chat_intent.py`、`test_hotspot_hook_library_gates.py`、`test_video_generation_ui.py`、`test_matching_diagnostics.py`、`test_hotspot_event_clips.py`。
2. 新增测试按 P2/P1/P3 自测小节；VM 无法跑 pytest 就交付"直接 import + 断言"结果。
3. 提交注明"批19 返工（续）"；**先把 diff 和测试结果回我复核，再算完成**。
