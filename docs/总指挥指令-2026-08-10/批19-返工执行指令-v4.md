# 批19 返工执行指令 v4（第三轮返工 · 外科手术式）

日期：2026-08-10
状态：第三轮验收 **FAIL**。本轮核心问题不是"实现有 bug"，而是**你报告的编辑与工作区实际内容严重不符**（详见第一、二节）。本指令改用**最小外科手术式改动 + diff 实证**，不再让你"重新实现"任何东西。

---

## 一、第三轮复核结论（我逐文件查的工作区，证据如下）

| 模块 | 我的实测 | 你的报告 | 判定 |
|---|---|---|---|
| P4 asyncio | `import asyncio` 在 admin.py:2 | 声称已加 | ✅ |
| P4 前缀 | `prefix="/api/admin"` | 声称已对齐 | ✅ |
| P4 路径 | 仍是 `/cleanup/run`、`/hook-cleanup/run` | 声称"路径对齐" | ❌ 未改 |
| P1 惩罚 | `_usage_freshness_penalty(last_used_at)` 已正确、无 `*2` | 声称已修 | ✅（这轮修对了） |
| P1 tie-break | sort 仍是 `int(item["hotspot_id"])`，candidate dict **无 `_tiebreak` 字段**，`random` 导了没用 | 声称"tie-break 已实现" | ❌ 未实现 |
| P2 | `_build_subtitle_cues_internal` **不存在**；`_detect_silence_points`/`_align_cues_to_silence` **各自重复定义两次**（:929/:1123、:979/:1168）；自造函数 `build_subtitle_cues_with_silence_detection`（v3 里没有）；中文标点正则仍是 `[,.!?;,!,？;,,]`；渲染调用点(:1721)用自造函数 | 声称"完全修复" | ❌ 更乱了 |
| P3 路径 | 端点仍是 `/api/video-projects/enqueue`（无 `{project_id}`），前端调 `/{project.id}/enqueue` | 声称"路径对齐" | ❌ 仍 404 |
| P3 表/API | 已用 `db.add_to_queue`，无 `db.execute`/`publish_queue` | 声称已改 | ✅（这轮修对了） |
| P3 scheduled_at | 仍是 `datetime.now(timezone.utc).isoformat()`（ISO 带 `T`） | 声称"填 ISO 格式 now" | ❌ 立即发布永不触发 |
| P3 属主/账号校验 | 无；`get_video_generation_job(project_id)` 直接把 project_id 当 job_id | 未提 | ❌ 未做 |
| 前端 body | 仍是 `{method:'POST'}`，无 body | 声称前端已改 | ❌ 未改 |

**核心诊断**：三轮下来，你的**叙述性总结与 `git diff` 实际内容对不上**。P1/P3/P4 的树状状态和上一轮几乎一样——说明你描述的大多数修改**没有落到这些文件里**（要么改到别的副本，要么没有真正保存）。P2 则是重复定义 + 自造函数名的混乱。**第三轮不是"修得不对"，是"报告了没做的事"。**

---

## 二、本轮纪律（必须遵守）

1. **不准重新实现、不准发明新函数名、不准"改进"。** 所有改动都是下面列出的**精确 diff**。
2. **每一步都要贴 `git diff` 实证**：只回"我改了"不算数。每完成一个模块，把 `git diff -- <文件>` 的**实际输出**贴出来。
3. 如果发现某个文件的代码和你预期不同，**停下来问我**，不要自己猜着改。
4. **强烈建议在全新会话/清空上下文后执行**——三轮偏差可能来自你的会话上下文污染。若当前会话能正常 `Read` 本文件，则无需换；若再出现"文档读不到/改了不生效"，请换新会话重开。

---

## 三、改动清单（按文件，外科手术式）

### 3.0 第一步：重置 P2 的混乱

```bash
git checkout -- video_renderer.py
```
这条命令把 `video_renderer.py` 恢复到批19 之前的干净版本（只有原始字符比例 `build_subtitle_cues`，没有任何 `_detect_silence_points`/`_align_cues_to_silence`/`build_subtitle_cues_with_silence_detection`/`_build_subtitle_cues_internal`）。
执行后先确认：
```bash
grep -n "_detect_silence_points\|_align_cues_to_silence\|build_subtitle_cues_with_silence_detection\|_build_subtitle_cues_internal" video_renderer.py
# 期望：无输出
```

### 3.1 P2：应用 v3 的 P2 终稿（严格逐字符）

重置后，打开 **`docs/总指挥指令-2026-08-10/批19-返工执行指令-v3.md` 第四节（四、P2：字幕静音对齐）**，把里面 **4.1~4.5 五个代码块**逐字符粘进 `video_renderer.py` 对应位置：

- 4.1 `build_subtitle_cues`（替换原函数）
- 4.2 `_build_subtitle_cues_internal`（**新增**）
- 4.3 `_detect_silence_points`（**新增**）
- 4.4 `_align_cues_to_silence`（**新增**，含 `sec_to_real` 的语音时间轴映射）
- 4.5 渲染调用点（把 `cues = build_subtitle_cues(scene["voiceover"], ...)` 改为 `cues = _build_subtitle_cues_internal(scene["voiceover"], ..., audio_path=audio_path, ffmpeg=ffmpeg or "ffmpeg")`）

**完成后必须满足（贴 grep 实证）**：
```bash
grep -n "_build_subtitle_cues_internal(" video_renderer.py          # ≥2 处（定义 + 渲染调用点）
grep -n "def _detect_silence_points\|def _align_cues_to_silence" video_renderer.py   # 各**恰好 1 处**
grep -n "build_subtitle_cues_with_silence_detection" video_renderer.py                # 无输出
grep -n "\[\，。！？；,.!?;\]" video_renderer.py                                      # ≥1 处
grep -n "sec_to_real" video_renderer.py                                               # ≥1 处
```

### 3.2 P1：只改两处（不要动别的）

**改动 A** — 在 `app.py` 的 candidate dict 里，`"score": (...)` 块之后、`"usage_boundary":` 之前，插入一行：
```python
            "_tiebreak": random.random(),
```

**改动 B** — `app.py` 排序 key（当前 :2137）第三元从：
```python
        key=lambda item: (item["score"], item.get("published_ts") or 0, int(item["hotspot_id"])),
```
改为：
```python
        key=lambda item: (item["score"], item.get("published_ts") or 0, item.get("_tiebreak", 0.0)),
```
（`reverse=True` 保持；其他一律不动。）

**实证**：
```bash
grep -n "_tiebreak" app.py        # 期望 ≥2 处
grep -n "int(item\[\"hotspot_id\"\])" app.py   # 期望：仅 4671 那处存在（那是另一函数），:2137 无
```

### 3.3 P4：改两个路径（routes/admin.py）

```bash
# 把
@router.post("/cleanup/run")
# 改成
@router.post("/media-cleanup")
# 把
@router.post("/hook-cleanup/run")
# 改成
@router.post("/hotspot-hook-library/cleanup")
```
（`import asyncio` 和 prefix 已正确，别动。）

**实证**：
```bash
grep -n "@router.post" routes/admin.py   # 期望：/media-cleanup 与 /hotspot-hook-library/cleanup
```

### 3.4 P3：替换 models 模型 + 端点 + 前端

**3.4.1 models.py** — 把 `VideoProjectEnqueueRequest`（:398）整体替换为（**去掉 project_id 字段**，id 走路径参数）：
```python
class VideoProjectEnqueueRequest(BaseModel):
    platforms: list[str] = Field(default=[], description="要发布的平台（Platform 枚举值字符串，如 douyin/xiaohongshu）")
    account_targets: dict[str, list[int]] = Field(default={}, description="平台 -> 目标账号主键列表")
    scheduled_at: Optional[str] = Field(None, description="计划发布时间；留空=立即发布")
    title: Optional[str] = None
```

**3.4.2 routes/video_generation_routes.py** — models import 块加 `Platform,`；然后**整段替换** enqueue 端点（:433 起）为：

```python
    @router.post("/api/video-projects/{project_id}/enqueue")
    async def enqueue_video_project(
        project_id: str,
        request: VideoProjectEnqueueRequest,
        user=Depends(get_current_user),
    ) -> dict:
        """将已完成渲染的视频项目加入发布队列（立即或定时）。"""
        project = db.get_video_project(project_id, created_by=user["id"])
        if not project:
            raise HTTPException(status_code=404, detail="Project not found")
        job = db.get_video_generation_job(project.get("active_job_id"))
        if not job or job.get("status") != "succeeded" or not job.get("output_path"):
            raise HTTPException(status_code=400, detail="成片尚未就绪，无法入队")

        now_str = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        scheduled_at = request.scheduled_at or now_str
        platforms = request.platforms or [project.get("platform", "douyin")]
        valid_platforms = {p.value for p in Platform}
        for platform in platforms:
            if platform not in valid_platforms:
                raise HTTPException(status_code=400, detail=f"不支持的平台：{platform}")

        created_ids = []
        for platform in platforms:
            target_ids = request.account_targets.get(platform) or [None]
            for target_id in target_ids:
                if target_id is not None:
                    account = db.get_account(target_id)
                    if not account or account.get("owner_id") != user["id"]:
                        raise HTTPException(status_code=403, detail="不能操作其他用户的账号")
                    if account.get("platform") != platform:
                        raise HTTPException(status_code=400, detail="目标账号与发布平台不匹配")
                queue_id = db.add_to_queue(
                    title=request.title or project.get("title") or "",
                    body="",
                    platform=platform,
                    scheduled_at=scheduled_at,
                    status="queued",
                    created_by=user["id"],
                    attachments=[{"type": "video", "path": job["output_path"]}],
                    target_account_id=target_id,
                )
                created_ids.append(queue_id)

        db.add_audit_log(
            user["id"], user["username"], "enqueue_video_project",
            target=project_id, detail=json.dumps({"queue_ids": created_ids}),
        )
        return {"status": "queued", "queue_ids": created_ids, "message": f"已入队 {len(created_ids)} 条"}
```

**3.4.3 static/video-project.html** — `enqueueToPublishQueue()` 函数体替换为带 body 的版本：

```js
async function enqueueToPublishQueue(){
  if(!project?.id){showToast('项目 ID 无效','error');return;}
  try{
    const payload={
      platforms:[project?.platform || 'douyin'],
      account_targets:{},
      scheduled_at:null,
    };
    const response=await apiFetch(`/api/video-projects/${project.id}/enqueue`,{
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body:JSON.stringify(payload),
    });
    const result=await response.json();
    if(response.ok){
      showToast('✅ 已成功加入发布队列，系统将在 1 分钟内自动发布');
      setTimeout(()=>location.reload(),1500);
    }else{
      showToast('❌ '+(result.detail||'入队失败'),'error');
    }
  }catch(error){
    showToast('❌ 加入失败：'+error.message,'error');
  }
}
```

**实证**：
```bash
grep -n "@router.post(\"/api/video-projects/{project_id}/enqueue\")" routes/video_generation_routes.py   # 期望：有
grep -n "db.execute\|publish_queue\|VideoGenerationJobStatus" routes/video_generation_routes.py          # 期望：无输出
grep -n "strftime(\"%Y-%m-%d %H:%M:%S\")" routes/video_generation_routes.py                              # 期望：有
grep -n "get_video_project(project_id, created_by=user" routes/video_generation_routes.py                # 期望：有
grep -n "body:JSON.stringify(payload)" static/video-project.html                                         # 期望：有
```

---

## 四、最终实证提交（缺一不可）

```bash
cd /Users/ylanlll/Desktop/商务部/distribution-manager
git diff --stat
git diff -- video_renderer.py app.py models.py routes/video_generation_routes.py routes/admin.py static/video-project.html
```
把这两条命令的**完整输出**贴回（这就是我复核的依据）。再跑一遍 v3 第七节的自检命令，贴输出。

## 五、回归

宿主有 pytest 则跑：`test_video_generation_ui.py`、`test_chat_intent.py`、`test_hotspot_hook_library_gates.py`、`test_media_retention.py`、`test_matching_diagnostics.py`、`test_hotspot_event_clips.py`；否则说明原因。

提交注明"批19 返工 v4"。**只认 diff 实证，不认叙述。**
