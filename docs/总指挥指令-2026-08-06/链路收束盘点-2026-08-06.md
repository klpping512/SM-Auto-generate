# 全套链路收束盘点 —— 2026-08-06

> 性质：总指挥对 08-04/05/06 三批改动的全链收束核验。
> 依据：`docs/总指挥指令-2026-08-04/`（11 项）、`-05/`（母片提速/多窗兜底/audit A/B）、`-06/`（策展 JSON dump+重试）、`docs/改进日志/2026-08.md`、git 历史、线上代码/DB 抽查。
> 状态：**链路设计已闭环**，剩余全为「挂账 / 待钥匙 / 待样本」类，无设计缺陷。

## 一、总评

整套链路（热点抓取 → 入库筛选 → 下载 → 镜头分析 → 策展 → 核验 → Hook 库 → 素材匹配 → 成片 → 质检重生成）**设计完整、代码全部落地**。全部 commit 已推 `codex/semantic-assets-mvp`，工作区干净。剩余项目要么是被刻意挂账（另立项目）、要么等外部钥匙（za-stock）、要么等自然事件（策展偶发失败样本），不阻塞链路运行。

## 二、全链健康度

| 子链 | 环节 | 状态 |
|---|---|---|
| **Hook 生产** | 信源 5 YT 定稿（无 SABC）+ RSS 清死源加 Freight | ✅ env 验真：eNCA/Newzroom/CNBC/BDTV/Transnet |
| | 抓取 6h 定时 | ✅ |
| | 入库选片（hotspot_hook_intake）JSON 加固 | ⏸ 另立项目 |
| | 下载（720p 封顶 / 代理 / 300s 超时） | ✅ |
| | Transnet 抓取硬化（retryable / evergreen / -F 预检） | ✅ H-hit_mother=40% |
| | 母片提速（先筛后下 / 低清分析 480 / 高清定稿 720 / 有界并发） | ✅ `34c66af` |
| | 镜头分析（多窗默认关、duration_seconds 不被覆盖） | ✅ `b69a10e` |
| | 策展 JSON 失败 dump + 一次绕缓存重试（诊断表落库） | ✅ `94fe241` |
| | 核验 critic | ✅ |
| **匹配/成片** | 匹配诊断仪表盘 | ✅ `8be5c70` |
| | 节点推导补覆盖（关税→清关等） | ✅ `7af451d` |
| | 清关/关税放闸 preparation 模式 | ✅ `7285c0e` |
| | 文案门禁 黑名单→白名单正向强制（真气密） | ✅ `99c4ecd` |
| | 渲染编码档位（crf18/medium） | ✅ `d886d7d` |
| | 质量闭环 人在环 + 可改轴加权分 | ✅ `1795fd5` + `628b61d` |
| | 词表统一（hotspot_lexicon + asset_taxonomy 阶段C） | ✅ `b95a231` |
| **素材供给** | Transnet 回填（头号资产，现场实拍） | ✅ P0 完成 |
| | za-stock 免版权管线（填 customs/facility） | ✅ 受控开闸：usable_source 放行 za_stock_license + 口播强制安全模板 + cap 20000 |
| **运维** | budget 死锁修复、stale-server 重启、日志落盘 | ✅ |

## 三、已闭环关键项（代码级验真）

1. **策展 JSON 残 bug**：新码下偶发。已落地 `hook_curation_diagnostics` 表 + `model_router.call_text(use_cache=...)` + 策展 `max_calls=1→2`（1 初始 + 1 绕缓存重试）+ `scripts/dump_hook_curation_diagnostics.py` 分类脚本 + 3 条新测。**已拍板不调 `max_output_tokens=1000`**（空表无截断证据，等下次偶发失败 dump 定性）。
2. **stale-server**：08-04 13:03 旧进程已重启（PID 39848），08-05 修复全部生效。验三事过：5 台频道 / duration 不覆盖 / 策展 JSON 可解析。
3. **保留频道 10 条 JSON 失败母片**（eNCA 7 + BDTV 2 + CNBC 1）已按 media-id 白名单 requeue → 10/10 `ready` + `no_qualified_hooks`；SABC 政治片当垃圾未重试。
4. **budget 死锁**：策展/核验 `reset=True`，Transnet 回填 `tech_fail=0`。
5. **文案门禁真气密**：#6 六句自然完成说法漏点 12/12 被白名单 `whitelist_forced` 强制；黑名单降级兜底扫 voiceover+text_overlay 双流。
6. **质量闭环**：护栏先于 auto/manual 分流评估、血缘回灌 history、可改轴加权分与 `overall_score` 并存、`quality_failed` 一字未动。
7. **词表统一**：物流词表 `hotspot_lexicon`、画面分类 `asset_taxonomy`（阶段C 真 alias，防隐性回退）。

## 四、剩余挂账（非设计缺陷）

1. **customs 供给侧已入库 24 且受控进匹配池**（za-stock manual/ready；口播强制安全模板；mixkit 仍排除）。
2. **入库选片 JSON 加固**（hotspot_hook_intake `_parse_selections`/`_parse_audit`）——另立项目。
3. **策展截断 vs 空返回定性**——诊断表空表（total=0），等下次偶发失败跑 dump。
4. **audit 快模型 A/B**（`audit-model-ab.md` 占位）——可选提速项，A/B 差异 <5% 且边界人工 OK 才开 `SA_HOTSPOT_AUDIT_FAST`。
5. 小尾巴：SABC 73 残余不清（有意，垃圾政治片）；914 下载 300s 超时挂账（BDTV 演播室低价值）；`video_evaluator` prompt 写 Qwen-VL 实为 mimo-v2.5（名实不符，一行字待对齐）。

## 五、关键读数（2026-08-06 DB）

- 媒体 1163（全 authorized），`ready` 324 / `processing_failed` 81 / `not_started` 757 / `processing` 1。
- `processing_failed` 91 → 81：10 条保留频道清零后，残余 = SABC 73 + SA Today 4 + SA Now 2 + Moneyweb 1 + CNBC 851（budget 类）+ SABC budget 1，全部已砍频道或 budget 类，非 JSON 截断问题。
- 确认 Hook 母片 104 → **122**（Transnet 回填 + 908-914 重跑持续出货）。
- 诊断表 `hook_curation_diagnostics` = 0 行（未触发解析失败）。
- git：全部已推 `origin/codex/semantic-assets-mvp`，工作区仅 `.claude/` `.qoder/` 工具目录。

## 六、后续行动（拍板）

1. **za-stock 管线**（Pexels+Pixabay key 已备）：`python3 scripts/pull_za_stock.py --category customs facility delivery` → `python3 scripts/ingest_za_stock.py` → GET `/api/diagnostics/owned-matching?topic=清关` 验收 customs 候选 0→正。**ingest 后重启 app** 新素材才进匹配池。
2. **策展偶发失败样本**：再崩一次 → `python3 scripts/dump_hook_curation_diagnostics.py --limit 30`，分类计数报总指挥；`截断为主` → 调 max_output_tokens/压缩证据；`空/纯错误为主` → 维持重试。
3. **入库选片 JSON 加固**立项（复刻本次 dump+重试模式）。
4. audit 快模型 A/B 可选跑，不掉质再开开关。
