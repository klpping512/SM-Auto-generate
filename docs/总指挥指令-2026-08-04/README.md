# 总指挥指令 · 2026-08-04

> 我（总指挥）出思路 / 审设计 / 写可粘贴的 Cursor 指令 / 验收；具体代码由 Cursor 执行。
> 本文件夹按日期归档当天产出的执行指令。状态：🟡待执行 · 🔵执行中 · 🟢待验收 · ✅已验收 · ⏸挂起

## 指令清单

| # | 指令 | 目标一句话 | 状态 | 备注 |
|---|------|-----------|------|------|
| 1 | [画面素材分类统一](画面素材分类统一-Cursor执行指令.md) · [阶段C](画面素材分类统一-阶段C-Cursor执行指令.md) | 把散在多处的画面素材分类词表收敛到单一 `asset_taxonomy.py`，修两份 `CATEGORY_KEYWORDS` 漂移 bug | ✅已验收(A+B+C) | 阶段 A+B 已验收（commit `0cbd945`+`d423323`）。**阶段C 已验收（commit `b95a231`+`9c00b95`）：总指挥下代码复核——`CATEGORY_TERMS = asset_taxonomy.CATEGORY_KEYWORDS`(asset_processing.py:101) 真 alias；真源已补 warehouse{装卸/terminal/container terminal}·delivery{港口/船舶/port}·staff{team}·facility{forklift}·customer{签收/delivery receipt}、customs 用「通关」、staff 已删「访谈」（asset_taxonomy.py:81-123 逐词核对无误）。执行者对抗式推翻我指令两处均成立：①我说"forklift/签收/team 不补"错——子串匹配下中文「叉车/反馈」命不中英文 OCR，按我自设判据应补，已补；②改动③brand 不注入{Buffalo}(会污染 brand_proof)改为显式注释。港口/forklift/team 素材入库分类前后一致不回退——验收通过。** 原阶段C 指令：探查确认第三份 `asset_processing.py:97 CATEGORY_TERMS`（入库真规则分类器 `_category_scores`→`classify_evidence`→`process_asset_job`）仍含「访谈」、customs 用「关税」漂移；第四份 `semantic_matching.py:116 category_tags` 是反向映射（分类→标签，形态不同不能合并）。**关键对抗发现**：直接 alias 会隐性回退——真源把「港口/船舶/port/terminal/装卸」放在能力标签而非主分类词表，直接等号会让港口素材 primary_category 掉到 other。∴ 指令定为**两步走**：①先给真源 delivery/warehouse 补回这 5 词 ②再 `CATEGORY_TERMS = asset_taxonomy.CATEGORY_KEYWORDS`（「访谈」随之删、关税→通关统一）；③`category_tags` 只对齐键集不合并，可拆下一 commit。无循环 import 风险 |
| 2 | [P0 匹配诊断仪表盘](P0-匹配诊断仪表盘-Cursor执行指令.md) | 匹配不足时暴露"饿在哪一侧、死在哪道闸"，停止盲目人工打标 | ✅已验收 | commit `8be5c70`+`364cb8d`。闸门谓词与 `_owned_candidates` 共享、零副作用、732 passed。**首跑 topic=清关**：verdict=category_mismatch，265 可用片过 customs 闸=0，库存全是 warehouse256/delivery83、**无 customs**；热点侧 4 hook 对得上。结论：清关是"严闸+零库存"内容缺口，语义向量救不了；两杠杆=放闸(抄末端 preparation 模式)/补内容 |

| 3 | [P1 话题扫描](P1-话题扫描-Cursor执行指令.md) | 扫 8 个代表话题，分清三种死法各占多少，再决定 P1 买哪副药 | ✅已验收 | commit `01ba0f1`+`cc2fc24`。**读表修正**：`healthy 7/8` 是假象——`app.py:4315` 节点推导只认 5 词、其余塌缩到「运输」(eligible=delivery)，故关税/港口/成本/断链 4 行 funnel 全同=没测到。真测到 4 个：清关(customs 缺口)/末端(healthy)/仓储(owned 够但 hotspot=0 饿热点侧)/干线(healthy)。**结论**：无"关键词漏配"证据→不买语义向量；真信号=customs 内容缺口+节点推导覆盖窄 |

| 4 | [P1.5 节点推导补覆盖+重扫](P1.5-节点推导补覆盖+重扫-Cursor执行指令.md) | 补「关税→清关(customs)」等缺失映射，重扫验证真实死法分布 | ✅已验收 | commit `7af451d`+`a5072ba`。预测 3/3 全中：关税跳成第 2 个 customs 缺口(0/0/owned/customs)、港口/成本/断链正确节点下真 healthy、无旧断言需改。**P1 诊断结案**：唯一 category_mismatch 来源=customs 内容缺口(清关+关税)，语义向量正式搁置 |

| 5 | [P2 渲染参数快赢](P2-渲染参数快赢-Cursor执行指令.md) | 正式片锐化：中间片近视觉无损(crf18)+交付段 medium 预设，减代际损失 | ✅已验收 | 4 处参数已改（`video_renderer.py`），新增 `RENDER_PRESET/RENDER_INTERMEDIATE_CRF/RENDER_FINAL_CRF` 环境档位。**真实 job A/B 实测**（a5104b2e 重放）：总耗时 38.0s→60.8s(+60%)、体积 14.5MB→20.0MB(+37.5%)、字幕文字边缘能量+5%~15%（拉普拉斯客观度量）、质量门禁全过；preview 路径仍 ultrafast+crf28 未变。单条 ffmpeg 最长 22.9s≪180s 子进程超时，`cleanup_stale_jobs` 仅启动时跑一次→300s 红线风险低；首个 50–90s 正式片仍需观察 |

| 6 | [P1 终局 清关放闸+文案门禁](P1终局-清关放闸+文案门禁-Cursor执行指令.md) | 清关/关税放闸(preparation)取用 warehouse/delivery 备货上下文，**同批**建确定性文案门禁堵住"仓库画面谎称已清关" | ✅已验收 | 三件套**同批提交**：①`_eligible_owned_categories` 清关节点→{customs,warehouse,delivery}（真实 brief 放闸前 candidates 0→3）②`safe_customs_preparation_copy` 安全兜底模板（纯准备词，无开场前缀防字数超限）③`overclaim_completion_issues` 纯确定性子串拦截器 + `apply_overclaim_guard` 接入 B1 生产链（命中回退模板并记 `overclaim_guard`）+ B2 预览链 locked_scenes 补 primary_category 同门禁。单测 13 项全绿：完成词 16 个全拦、准备词 7 个全放行、真 customs 素材/非清关节点不误杀；B1 端到端证「模型说已清关→回退→成片字幕无完成词」。被放闸反转的 3 条存量断言已按新行为改写（staff/facility 仍保持 mismatch 拒绝）；全量回归除 8 个存量基线失败外全绿。**总指挥对抗式复核（commit `7285c0e`）**：接线确在真 B1 渲染路径、voiceover+overlay 双流回退、owned 镜头工厂全设 primary_category 无漏、3 条改写测试系加强覆盖非放水——**验收通过**。**⚠️但"气密"下调一格**：③是有限子串黑名单，只对列举完成词气密、非语义保证；独立实测 6 句自然完成说法漏过（`已出关/通关手续办妥/顺利过关/货物已顺利放行/海关放行了货物/已经清关完毕`）。受监管宣称残余风险仍在。**真气密正解=翻白名单/正向强制**（非-customs 素材在清关节点强制走安全模板），待总指挥定：黑名单扩词（廉价补丁）vs 白名单重构（真气密、牺牲文案多样性）。 |

| 7 | [P3-A 人在环辅助重生成 v1](P3-A-人在环辅助重生成v1-Cursor执行指令.md) | 把质检诊断在审片页露出（死数据变活）+ 复用 resume 重生成 + 接通 history 让重跑可度量有护栏 | ✅已验收 | 三件同批提交：件1 审片页「AI 质检诊断」面板（总分+Δ、逐轴低分高亮、issues 逐条 severity/suggested_fix、文生视频建议带免责声明）；件2 「按质检建议重生成」→带建议面板的脚本编辑→复用 resume（后端零新通道）；件3 **血缘落 job 表新列** prior_job_id/regen_attempt（_ensure_column 迁移）+ `collect_prior_quality_history` 沿链回灌 history。**对抗审查发现指令低估的死结**：`decide_regeneration` 护栏分支全在 `if not auto_enabled` 短路之后，不重构则护栏永空转——已重构为护栏先于 auto/manual 分支评估（阈值/max_attempts/improvement 默认值未动）；无 history 时保持旧 `automatic_regeneration_disabled` 语义。护栏四态单测全绿（提升<3→no_meaningful_improvement/达上限→maximum_attempts_reached/下滑→score_declined/护栏通过→manual_regeneration_allowed）+ 血缘两轮 resume 链式回灌端到端；前端达上限/提升不足禁用重生成按钮并给原因；`auto_regenerate=False` 未动、未做局部重渲。全量 764 passed，剩 8 存量基线失败。**总指挥对抗式复核（commit `1795fd5`）**：控制器重构逐态手验正确（旧 `automatic_regeneration_disabled` 语义没破、默认值未动）；接缝准（`run_quality_mvp` 本就收 history，生产从没传值，补回灌即通）；血缘/环防护/前后端禁用集一致——**验收通过**。小注：`score_declined` 只警示不禁用（下滑后仍可在 2 次上限内再试）。**诚实定性**：P3-A 只接通闭环的「度量+护栏」半边，重跑质量本身仍取决于人工编辑脚本+素材是否够，不自动变好——真正天花板仍回到 customs 内容缺口 |
| 9 | [P3-B 评估器可改轴加权分](P3-B-评估器可改轴加权分-Cursor执行指令.md) | 把"该不该重生成"从盲信单个 `overall_score` 改为看重生成改得动的四轴加权分（并存，不替换） | ✅已验收 | **总指挥下代码复核（commit `628b61d`+`bd40dab`）：`quality_failed`(schemas.py:76-83) 一字未动仍只读 overall_score、pass/fail 零变化；加权分随所有失败分支带出、`actionable_axes_healthy`(regeneration_controller.py:61) 正确卡在四道 P3-A 有界护栏之后·auto/manual 分流之前、已入 BLOCKING_REASONS(L16)。执行者对抗式推翻我指令三处(算式实为 68.5/helper 预警落空/无history排序矛盾)经手验全部成立，是真发现非放水——验收通过。** 已执行：schemas 新增 `ACTIONABLE_AXIS_WEIGHTS`(subtitle_audio .35/prompt_align .30/storytelling .20/platform .15，和=1.0，画面六轴=0)+纯函数 `weighted_actionable_score`；`decide_regeneration` 在四道有界护栏（quality_passed/达上限/下滑/提升不足）**之后**、auto/manual 分流**之前**接入 `ACTIONABLE_FLOOR=70` 判定，达标→`manual_review/actionable_axes_healthy`（入 BLOCKING_REASONS，自动与人工重跑一并挡住）；加权分随所有失败分支带出供前端展示。`quality_failed`/`route_*`/merge/归一化兜底一字未动，pass/fail 零变化。**对抗审查三处修正指令**：①指令测试算式 0.35*80+0.30*70+0.20*60+0.15*50 实为 68.5 非 68.0；②两处 `_report` helper 本已填全 10 轴，无需补；③测试例 2（无 history 判 actionable_axes_healthy）与"新 reason 在五既有 reason 全不命中后"字面矛盾，以用例为准：判定拦在 automatic/manual_regeneration_disabled 之前（含拦人工重跑，符合"挡住无意义人工重跑"语义条款），3 条非保护存量断言改以 actionable=60 构造维持原命题。新增 `tests/test_weighted_actionable_score.py`（8 例）；全量 788 passed，8 个失败经 git stash 对照确认为存量基线 |
| 10 | [文案门禁升级 黑名单→白名单](门禁升级-黑名单转白名单正向强制-Cursor执行指令.md) | 把 #6 的确定性黑名单翻成白名单/正向强制，堵住 6 句自然完成说法漏点（真气密） | ✅已验收 | **总指挥下代码复核（commit `99c4ecd`+`72b09ca`）：`requires_safe_customs_copy`(hotspot_preview_narration.py:318) 纯确定性只看类别×节点；`apply_overclaim_guard`(L349) 白名单强制在前(L373-389,无条件替换且同时改 voiceover+text_overlay)·黑名单兜底在后(L390-406,未删,且兜底扫 voiceover+overlay 双流堵住原字幕单独漏词)；B1(app.py)/B2(generate_narration) 调用点一字未改。#6 六句漏点 12/12 全被 whitelist_forced 强制、真 customs/非清关节点不误伤——验收通过。执行者额外发现"旧黑名单只替 voiceover、字幕单独漏"并一并堵住，属指令未提的真漏点。** 续 #6：黑名单只对枚举完成词气密，实测 6 句漏过（已出关/通关手续办妥/顺利过关/货物已顺利放行/海关放行了货物/已经清关完毕）。**白名单正解**：借用清关上下文的非-customs 素材(warehouse/delivery/staff/facility × 清关节点)**无条件强制走 `safe_customs_preparation_copy`**，不检测文本=剥夺自由宣称机会。黑名单降级为第二道防线（防御纵深不删）。**代价**：被强制 scene 失文案多样性，但严格限定在"借来上下文"这一小撮危险 scene，真 customs/非清关节点不受影响。同址接入(`apply_overclaim_guard`+两调用点)，调用点不改 |
| 8 | [信源调整](信源调整-方案与Cursor执行指令.md) | 把泛综合信源调成物流/商业垂直为主，清死源，提高母片物流命中率 | 🟢待验收 | 四步门禁已过并已 push。**真 H-hit（铁律E）**：策展分母6、确认Hook0、H-hit_mother=0%；tech_fail=0；Transnet 3条单视频不可用（`This video is not available`）未入分母。见 `step12-hhit-final.json` |

## 背景速记（为什么做这些）

- **匹配失败 + 视频质量差 是同一个根**：系统无 AI 文生视频，成片是 FFmpeg 拼真实素材；匹配不足会触发自适应降级出片，降级片就是"质量差"。修匹配同时治两病。
- **两侧素材性质不同**：热点素材每 3 天批次抓取、切几秒 hook（时效/覆盖问题，打标无效）；Buffalo 自有素材静态、靠人工打标（可打标修）。诊断必须区分。
- 昨日已升级 MIMO V2.5 PRO 等模型——只改善"大脑"（脚本/打标/打分），改善不了"画面本身"。

## 调优路线图（性价比排序）

- **P0** 匹配诊断仪表盘 ← 指令 #2（先跑，最便宜，防止力气使错方向）
- **P1 已扫，结论=不买语义向量**。8 话题扫描后真测到 4 个，无"闸内有货却关键词漏配"证据。真实病灶两条，都不是匹配精度：①**customs 内容缺口 + 最严闸**(清关；关税补上节点覆盖后大概率也塌)；②**节点推导覆盖窄**(`app.py:4315` 仅认 清关/末端/配送/仓储/运输，其余塌缩到运输→delivery，卡在最上游)。
- **P1.5 下一步(最高杠杆、便宜)**：补齐节点推导覆盖(关税→customs、港口→delivery/port 等)→重扫关税/港口/成本/断链验证真实死法→再定终局。预测终局=**customs 补内容 + 清关/关税放闸(抄末端 preparation 模式)**，非重写算法。
- 另一条独立轨：**仓储 hotspot_pool=0** = 热点抓取覆盖问题(批次 07-24 无仓储 hook)，属抓取侧非打标侧。
- **P2** 渲染参数快赢：字幕烧录 crf 28→18–20、preset veryfast→medium、TTS 加音色（立竿见影）
- **P3** 质量闭环：评估权重偏向可改轴、重生成应重选素材而非同输入再渲
