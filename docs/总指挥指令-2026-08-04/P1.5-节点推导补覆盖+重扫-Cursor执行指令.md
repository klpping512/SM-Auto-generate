# Cursor 执行指令 · P1.5 节点推导补覆盖 + 重扫验证

> 总指挥背景（先读再动手）：
> P1 扫描发现"healthy 7/8"是假象——`app.py:_chat_video_logistics_nodes` 把 topic 关键词
> 用硬编码 5 词集 `{清关,末端,配送,仓储,运输}` 做成员过滤，凡不在集内的话题一律塌缩到
> `["运输"]`（→ eligible=delivery）。经核，4 个塌缩话题里**只有「关税」是真语义错**
> （关税属海关事务，本该 eligible=customs，却被喂了 delivery 素材）；港口/成本/断链
> 塌缩后恰好落在合理的 delivery。
>
> 本指令目的：**把「关税」等缺失映射补上**，让节点推导对得起真实语义，然后**重扫**
> 关税/港口/成本/断链，验证——预测：关税会跳成第 2 个 `category_mismatch`（customs 缺口），
> 港口/成本/断链维持 healthy（delivery 素材充足）。若如预测，即实锤"真病灶=customs 内容
> 缺口，而非关键词漏配"，语义向量方案出局。
>
> **机制已核实（照做即可，不必再推敲）：**
> - `_topic_keywords` = `hotspot_lexicon.topic_keyword_hits`，返回命中的**原始词**（如 `["关税"]`）。
> - `app.py:4291-4294` 只保留原始词 ∈ `{清关,末端,配送,仓储,运输}` 的，其余丢弃 → 落 `["运输"]` 兜底。
> - `hotspot_video_planner._eligible_owned_categories:160`：节点串 ∈ `{"清关","customs","关税"}` → 返回 `{customs}`。
>   ∴ 只要让本函数发得出节点 `"清关"`（或 `"关税"`），下游就正确解析为 customs。

---

## 改动点（唯一）：`app.py` 函数 `_chat_video_logistics_nodes`

**定位**：该函数末尾，现有若干 `if any(term in ... ): nodes.append(...)` 证据块之后、
`return list(dict.fromkeys(nodes)) or ["运输"]` 这一行**之前**。当前长这样（锚点）：

```python
    if any(term in " ".join((topic, evidence_text)).casefold() for term in ("道路", "路况", "路线", "卡车", "r60")):
        nodes.append("配送")
    # The Hook curator has already confirmed a logistics bridge.  Without a
    # narrower node, use transport only as a preparation/action category; the
    # planner prompt still forbids claiming a completed delivery outcome.
    return list(dict.fromkeys(nodes)) or ["运输"]
```

**改为**（在 `return` 前插入三个新证据块，沿用本函数既有 append 惯例）：

```python
    if any(term in " ".join((topic, evidence_text)).casefold() for term in ("道路", "路况", "路线", "卡车", "r60")):
        nodes.append("配送")
    # 关税/进口税属海关事务：映射到清关节点，让 owned 匹配要求 customs 证据，
    # 而不是把仓库/运输画面默认当作关税话题的合法素材（否则 eligible 会误落 delivery）。
    combined = " ".join((topic, evidence_text)).casefold()
    if any(term in combined for term in ("关税", "进口税", "出口税", "tariff", "duty")):
        nodes.append("清关")
    # 港口/码头作业属运输能力；显式补上，避免它靠 ["运输"] 兜底才凑巧命中。
    if any(term in combined for term in ("港口", "港区", "码头", "港口作业", "港口码头", "port", "harbour", "harbor", "海运")):
        nodes.append("运输")
    # 供应链中断/罢工/拥堵直接改变提货、入库前准备与分拨安排：归到配送节点，
    # 由规划器用仓内/人员实拍解释可见准备动作；文案门禁仍禁止承诺时效或路线结果。
    if any(term in combined for term in ("中断", "断链", "供应链", "停摆", "罢工", "拥堵", "延误", "disruption", "strike", "congestion")):
        nodes.append("配送")
    # The Hook curator has already confirmed a logistics bridge.  Without a
    # narrower node, use transport only as a preparation/action category; the
    # planner prompt still forbids claiming a completed delivery outcome.
    return list(dict.fromkeys(nodes)) or ["运输"]
```

**注意：物流成本故意不映射**——它没有专属画面节点，落到 `["运输"]`（delivery 上下文）是诚实
且合理的默认。不要为它硬造节点。

---

## 硬边界（不得越界）

- 只在 `_chat_video_logistics_nodes` **追加** append 块；**不删**已有 5 词过滤、**不动** `or ["运输"]` 兜底。
- **不改** `hotspot_lexicon.py`（NODE_EXPANSIONS / topic_keyword_hits）、**不改** `hotspot_video_planner.py`、
  **不改** `asset_taxonomy.py`。本次是"话题→节点"这一层的桥接补全，不动词表真源。
- 不新增诊断逻辑、不改端点、不改选片/排序/阈值。

---

## ⚠️ 这是会上生产的行为变更（总指挥已知情）

改完后 `_chat_video_logistics_nodes` 是生产路径（`_chat_video_delivery_readiness` 也调它）。
**「关税」话题会从 eligible=delivery 收紧为 eligible=customs**：
- 正确性↑：不再把仓库画面冒充关税/海关证据。
- 副作用：关税话题在 customs 库存为 0 时会**匹配不足→触发自适应降级/需补素材**（这正是要暴露的真相，不是 bug）。

这是预期中的"用正确性换出真实缺口"，后续由"清关/关税放闸(preparation 模式)"或"补 customs 内容"解决。

---

## 测试

1. 跑全量 `pytest`。
2. **若有测试断言旧的「关税→delivery/运输」行为而失败**：**不要**擅自改写测试来迁就新行为，
   把"测试文件名 + 用例名 + 旧断言 vs 新实际值"回报总指挥定夺。其余无关失败沿用既有基线。

---

## 重扫（复用现成脚本，无需改脚本）

`scripts/sweep_matching_diagnostics.py` 的 `TOPICS` 已含 关税/港口/物流成本/供应链中断，直接重跑：

```bash
export DIAG_COOKIE='<你的 admin Cookie>'
python scripts/sweep_matching_diagnostics.py
```

覆盖 `docs/总指挥指令-2026-08-04/sweep-result.json`（旧值已在 git 历史，可对比）。

---

## 验收清单（Cursor 自检后回报）

- [ ] 仅改 `_chat_video_logistics_nodes`，追加 3 个 append 块，未删旧过滤/兜底
- [ ] 未改 lexicon / planner / taxonomy / 端点 / 脚本
- [ ] `pytest` 结果：全绿，或仅有"旧关税行为"类失败并已如实回报（未擅改）
- [ ] 重扫完成，覆盖 sweep-result.json

## 回报格式（给总指挥拍板 P1 终局）

贴回重扫后的 Markdown 表，并逐条对照预测：
- **关税** verdict 是否变为 `category_mismatch`、starving 是否 `owned`、eligible 是否 `customs`？（验证靶心）
- 港口 / 供应链中断 / 物流成本 是否 healthy？
- 一句话结论：customs 缺口是否为唯一/主要 `category_mismatch` 来源。
