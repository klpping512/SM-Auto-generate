# 总指挥指令登记 — 2026-08-07

| # | 批次 | 状态 | 摘要 |
|---|---|---|---|
| 16 | 批16 内容资产库面修复（Hook 分区 + 媒体格去噪 + 停 RSS 图片灌入） | **已执行（qcoder，commit ba62323 已推送）+ 总指挥验收通过 2026-08-07** | 治 08-07 三件事：①"几乎全部无 Hook"墙 = 794 张 RSS 图片噪音卡 + 288 张模型正常拒收卡 → 媒体格默认排除 image + 默认"全部素材"视图；②"混进 buffalo 素材" = 批12 generic_logistics 常青开场池 14 条（buffalo 自有 8 + za-stock 6）以"热点 Hook"卡混在新闻 Hook 区 → 按 hook_kind 拆分独立"常青开场池"区块 + 打标；③生产链链路没坏（匹配链功能隔离已核验），坏在库面 → 纯展示层修复。验收：API 口径 98（84 timely + 14 generic）✅；停灌验证（image 跳过/video_link 保留/794 存量未删）✅；pytest 全量 885 passed / 8 存量基线失败不变 ✅；app 已重启（PID 85567）。遗留：①浏览器目检 + 6h 调度停灌终确认留自然周期；②"素材池"标签现恒显示"内部素材"（`_decorate_hotspot_event` 未带 source_label，批17 A1 闭环）。文件：`批16-内容资产库面修复-qcoder执行指令.md`。 |
| 17 | 批17 热点 Hook 生产打标签 + 时效入链（卡片三标签 + 匹配新鲜度 + published_at 入链） | **已执行（qcoder，commit 96c8b64 已推送）+ 总指挥对抗式验收通过 2026-08-07** | 落实"分析好的热点素材直接打标签、保时效性/效率"：A 卡片打标签（时效徽标今日~超30天/常青 + 场景中文 chips + 来源标签，`_decorate_hotspot_event` 补父热点 published_at 与 source_label——**顺带闭环批16 遗留的素材池标签**，实测 14 条常青 = Buffalo 原有 8 + 免版权 6）；B 时效入匹配链（timely_event 加分 <24h+8/<3d+5/<7d+2/≥30d−3，排序键从乱序字符串 published_at 改为 published_ts）；C published_at 入链修复（下载路径捕获 upload_date 前进回填 + 新脚本回填 422 条 youtube 空日期旧热点）。验收：API 口径 98（84+14）含 published_at/source_label ✅；回填 scanned 402 / filled 394 / failed 8（premieres 未发布视频）——空日期 425→11、youtube 源 422→8、confirmed timely 父热点空日期 151/154→0/106 ✅；匹配对照实验（批17 vs 批16 基线）行为一致零回归 ✅；pytest 全量 885 passed / 8 存量基线不变 ✅；app 已重启（PID 67647）。文件：`批17-热点Hook生产打标签与时效入链-qcoder执行指令.md`。 |
| 18 | 批18 热点链路动线化改造（planner 时效感知开场 + 跨父中段递进 + 常青防冒充文案） | **已执行（qcoder，commit a23cec4）2026-08-07** | 落实"非固定顺序、动线流畅"进编排层：H 数据接入（`list_hotspot_event_clips` 并入父热点 `parent_published_at`；app.py 生成接口/chat 一键两处并入跨父 approved/locked 事件，过 `_is_confirmed_renderable_hotspot_hook` 门禁，解决静默丢弃）；I planner 动线（`_event_ts`/`_event_urgency` 镜像批17 档位并入 `_event_score`；替换固定分槽：新鲜度最高 Hook 开场 → 同源紧跟 → 证明段 → 异源 escalation 中段 → 图桥；scene 带 `hook_kind`+`flow_role`；`_voiceover` 对 generic/过期 timely 走常青模板、escalation 走"除此之外…"防混源）；J 新 `tests/test_hotspot_freshness.py`（13 用例：_event_ts 8 边界 + urgency 分档 + A/B/C/D 四套动线 + guardrail 回归）。裁掉项按总指挥评审：za_stock 准入不动、不加 `_marketing_hook_candidates` 硬门禁。验收：pytest 全量 898 passed / 8 存量基线失败（worktree 在 96c8b64 复验逐条一致）✅；数据层 168 条全带 `parent_published_at`，timely 151/151 父热点有日期 ✅；真库 planner 冒烟：开场 fresh timely → 证明段 → 异源 escalation 中段递进 ✅；app 已重启（新 PID 64869，项目无 /api/health 路由，以 / 200 + 启动日志验证）。文件：`批18-热点链路动线化改造-qcoder执行指令.md`。 |
