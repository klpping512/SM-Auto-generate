# 批22：云端源码部署与 15→50 条 Hook 执行回执

执行时间：2026-08-12 15:04 CST 起

执行目标：将批22完整生产链路部署到云服务器，保留 Buffalo 原有素材库；在云端按真实下载、内置模型分析、视觉/文本门禁产出 Hook，不以模型文案或未分析素材冒充合格 Hook。

## 一、部署结论

- 目标服务器：`139.159.239.137`，项目目录 `/opt/distribution-manager`。
- 云端系统：Linux x86_64；`salogiflow.service` 已重启并保持 active。
- 公共页面：`/`、`/assets.html`、`/hotspots.html` 均返回 HTTP 200。
- Buffalo 原有素材库：保留；未执行清空 `static/assets`，未删除原素材。
- Hook 周期清理：保持关闭（`HOTSPOT_HOOK_CLEANUP_ENABLED=0`）；未开启实删。
- 部署前备份：
  - `backups/batch22-cycle-deploy-20260812-135534`
  - `backups/batch22-cycle-deploy-20260812-135619-patch`
  - `backups/batch22-ytdlp-android-fix-20260812-1435`

## 二、本次上云内容

1. 以完整批22生产基线同步热点抓取、授权信源、媒体下载、镜头分析、Hook 策展和审计链路。
2. 接入 12 天完整周期清理检查：周期门禁未通过时只预览/告警；不再用单条素材年龄代替周期清理。
3. YouTube 云端代理适配 Android player client，修复 web client 偶发连接重置/格式不可用；不携带 Cookie，不绕过 DRM。
4. 保留南非官方机构与可信新闻视频源的授权配置；抓取仍受授权状态和真实下载结果约束。

主要文件：`scheduler.py`、`inspiration_assets.py`、`.env.example`、`hotspot_hook_cycle_cleanup.py`、`tests/test_hotspot_hook_cycle_cleanup.py`。

## 三、Hook 实跑回执

部署前云端审计：

- 合格时效 Hook：`1/50`。
- 唯一通过项：Hook 17「雪天N3高速持续行车」；人工看图与标题一致。
- 其他通用素材：14 条；不计入“近30天真实时效 Hook”。

本次新增实跑：

| 素材 | 来源 | 下载 | 模型分析 | confirmed Hook | 结果 |
|---|---|---:|---:|---:|---|
| 170 | South Africa Now | 成功 | 完成 | 0 | 模型未筛出可复用 Hook |
| 151/154/158 | BusinessDayTV | 成功 | 完成 | 0 | 模型未筛出可复用 Hook |
| 472 | SABC News「Traffic update」 | 成功 | 完成 | 0 | 模型未筛出可复用 Hook |

说明：下载成功不等于 Hook 合格；只有母片完成分析，并通过事件真实性、画面匹配、时效和素材状态门禁，才计入目标数量。批22目前尚未达到 50 条，不能写成“已过关”。

## 四、自动执行策略

- 每 3 天：抓取授权信源并将符合状态门槛的视频交给内置模型分析。
- 每 12 天：检查完整周期；只有周期记录、审计证据和数量门禁同时通过，才允许处理上一个完整周期的派生 Hook。
- 当前清理策略：默认 dry-run；达到运营验收条件后，由管理员显式开启实删。
- 运行期间的人工复核：优先检查视频首/中/末帧与标题、事件说明是否一致；发现错配立即下架，不回填虚假文案。

## 五、后续执行顺序

1. 继续使用云端可达的官方/可信视频源，优先港口、道路、边境、仓储、运输现场，不将泛新闻或棚内口播当作物流 Hook。
2. 每批记录候选、下载成功、分析成功、视觉通过、文本通过、confirmed 数量和失败原因。
3. 先补到 50 条合格真实时效 Hook，再启用 3 天增量与 12 天周期清理。
4. 任何一条与画面不一致的素材都不计入目标；不以“模型生成文案”替代现场证据。

## 六、验收

- 本地完整批22分支：热点周期清理、信源、抓取回归共 `37 passed`；YouTube 下载选项 `16 passed`；编译检查通过。
- 云端周期清理专项：`7 passed`。
- 云端服务与页面：服务 active，三个公共页面 HTTP 200。
- 本回执状态：`部署完成；Hook 补库进行中；15→50 未完成；实删未开启`。
